import { MbButton } from "mintbase-ui";

const Example = () => {
  return (
    <div className="flex items-center w-full justify-center h-screen">
      <MbButton label="Connect NEAR" />
    </div>
  );
};

export default Example;
